<div class="container">


<div class="page-header">
  <%= link_to context_submissions_path(@context), :class => 'btn btn-default' do %>
    <i class="fa fa-level-up fa-lg"></i>
    <%= t('context.submissions.my_list_title') %>
  <% end %>
  <%= link_to @submission, :class => 'btn btn-primary' do %>
    <i class="fa fa-info fa-lg"></i>
    <%= t('context.edit_submission.show_btn') %>
  <% end %>
  <h1><%= t('context.edit_submission.page_title') %></h1>
</div>


<%= render 'submission_revisions/form' %>

<%= render 'form' %>


</div>